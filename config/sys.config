%% -*- mode: erlang -*-
[
    {kernel, [
        {logger, [
            {handler, debug, logger_std_h, #{config => #{file => "anno.log"}}}
        ]}
        %{inet_dist_listen_min, 9100},
        %{inet_dist_listen_max, 9200}
    ]}
].

